services:
  nifi:
    image: apache/nifi:latest
    container_name: nifi
    ports:
      - "8443:8443"
    environment:
      - SINGLE_USER_CREDENTIALS_USERNAME=admin
      - SINGLE_USER_CREDENTIALS_PASSWORD=qwertyuiopo1
      - NIFI_WEB_PROXY_HOST=localhost:8443,127.0.0.1:8443
    volumes:
      #Volume per i file CSV
      - ./dati_csv:/opt/workspace/input_data
    restart: always